diff --git a/diagram/05_sequence_signal_read.puml b/diagram/05_sequence_signal_read.puml
index 4212075..641bd4a 100644
--- a/diagram/05_sequence_signal_read.puml
+++ b/diagram/05_sequence_signal_read.puml
@@ -4,6 +4,9 @@ title Signal Read Flow — CAN Bus → Dashboard
 
 actor "Vehicle ECU\n(Simulator)" as ecu
 participant "CAN Bus\n(vcan0 / vcan1)" as bus
+participant "CAN recv thread" as recv_thread
+participant "AppRunner\n(runner.py)" as runner
+participant "DatabaseLoader\n(parser.py)" as parser
 participant "CAN Reader[i]\n(reader.py)" as reader
 queue "asyncio.Queue\n(shared, all channels)" as q
 participant "Signal Processor\n(pipeline.py)" as proc
@@ -13,9 +16,9 @@ participant "WebSocket\n(websocket.py)" as ws
 actor "Dashboard\n(Browser)" as dash
 
 == Startup (per CAN channel) ==
-note over reader
-  AppRunner creates one CANReader per channel
-  (config.can is a list of CANConfig)
+note over runner
+  AppRunner loads per-channel DatabaseLoader and
+  creates one CANReader per channel (passes db_loader)
 end note
 reader -> bus : connect(interface, channel, bitrate)
 activate reader
@@ -25,8 +28,10 @@ note right of reader : Queue policy: drop_oldest | reject
 == Continuous Read Loop ==
 loop every CAN frame
     ecu -> bus : CAN frame (ID, data)
-    bus -> reader : bus.recv() via run_in_executor
-    reader -> reader : DatabaseLoader.decode_frame(msg_id, data)
+    bus -> recv_thread : recv() (blocking in recv thread)
+    recv_thread -> reader : call_soon_threadsafe(enqueue DecodedFrame)
+    activate reader
+    reader -> reader : decode using db_loader.decode_frame(msg_id, data)
     reader -> q : put(DecodedFrame)
 end
 
diff --git a/diagram/06_sequence_signal_write.puml b/diagram/06_sequence_signal_write.puml
index cffee6b..0039ff2 100644
--- a/diagram/06_sequence_signal_write.puml
+++ b/diagram/06_sequence_signal_write.puml
@@ -1,4 +1,5 @@
-@startuml Sequence - Signal Write Flow
+
+@startuml
 !theme plain
 title Signal Write-back Flow — Dashboard → CAN Bus
 
@@ -36,7 +37,10 @@ end
 
 == Rate Limit Check ==
 api -> api : check rate limit
-note right of api : Prevents CAN bus spam\nConfigurable per signal
+note right of api
+  Prevents CAN bus spam
+  Configurable per signal
+end note
 alt rate limit exceeded
     api --> dash : 429 Too Many Requests
 end
@@ -45,7 +49,10 @@ end
 api -> api : check shutting_down flag
 alt system is shutting down
     api --> dash : 503 {"error":{"code":"SERVICE_UNAVAILABLE","status":503}}
-    note right of api : Reject writes during\ngraceful shutdown (req 2.8)
+    note right of api
+      Reject writes during
+      graceful shutdown (req 2.8)
+    end note
 end
 
 == Write to CAN Bus ==
@@ -57,9 +64,15 @@ alt signal not writable
 end
 
 api -> writer : send_signal("BrakePressure", 45.0)
-note right of writer : CANWriterRouter routes to
-the correct CANWriter by signal name
+note right of writer
+  CANWriterRouter routes to
+  the correct CANWriter by signal name
+end note
+
 writer -> writer : encode(signal, value)\nusing can.json definitions
+writer -> writer : acquire asyncio.Lock
+writer -> writer : loop.run_in_executor(None, bus.send, msg) (threadpool send)
+
 writer -> bus : bus.send(can.Message(id=0x..., data=...))
 alt bus.send fails (BusError / timeout)
     writer --> api : CAN Write Error
diff --git a/diagram/07_sequence_websocket.puml b/diagram/07_sequence_websocket.puml
index 357690c..c49b0a0 100644
--- a/diagram/07_sequence_websocket.puml
+++ b/diagram/07_sequence_websocket.puml
@@ -1,4 +1,4 @@
-@startuml Sequence - WebSocket Lifecycle
+@startuml
 !theme plain
 title WebSocket Connection Lifecycle — Subscribe, Stream, Reconnect
 
@@ -16,6 +16,7 @@ note right of client
   New per-signal endpoint:
   ws://host/ws/subscribe
 end note
+
 ws -> ws : auth.verify(api_key)
 alt invalid key
     ws --> client : Close(4401, "Unauthorized")
@@ -27,7 +28,9 @@ end
 ws -> store : get_snapshot()
 store --> ws : {all signals with values}
 ws -> client : {"type":"signal", "signal":"VehicleSpeed", "value":84.1, ...}
-note right of ws : One message per signal in snapshot
+note right of ws
+  One message per signal in snapshot
+end note
 
 == 3. Real-time Streaming ==
 loop continuous
@@ -44,28 +47,39 @@ ws <- ws : broadcast_metrics(metrics_dict)
 ws -> client : {"type":"metrics",\n "cpu_percent":23.4,"ram_percent":41.0,...}
 
 == 6. Heartbeat ==
-note over ws : Runs concurrently (asyncio task)
+note over ws
+  Runs concurrently (asyncio task)
+end note
 loop every ws_heartbeat_interval_sec (e.g. 0.5s)
     ws -> client : {"type":"heartbeat",\n "timestamp":..., "uptime_sec":...}
 end
 
 == 7. Per-Signal Subscribe (/ws/subscribe) ==
-note over client : Alternative: use /ws/subscribe for
-selective signal streaming
+note over client
+  Alternative: use /ws/subscribe for
+  selective signal streaming
+end note
+
 client -> ws : ws://host/ws/subscribe
 ws --> client : Connection accepted
+
 client -> ws : {"action":"subscribe",\n "channels":["EngineSpeed","alarms","metrics"],\n "mode":"continuous"}
+
 ws -> ws : acquire _lock, mutate _ClientSubscription
 ws --> client : {"type":"subscribe_ack"}
+
 store -> ws : signal update ("EngineSpeed", 2750.0)
 ws -> ws : filter: client subscribed? YES
 ws --> client : {"type":"signal","signal":"EngineSpeed","value":2750.0,...}
 
 == 8. Disconnection & Reconnect ==
 client ->x ws : Connection lost
-note over client : Detect: no heartbeat
-client -> client : Reconnect with\nexponential backoff\n(1s → 2s → 4s → ... → 30s max)
+note over client
+  Detect: no heartbeat
+end note
+
+client -> client : Reconnect with\nexponential backoff\n(1s -> 2s -> 4s -> ... -> 30s max)
 client -> ws : ws://host/ws/signals?api_key=<key>
 ws --> client : Connection accepted
 
-@enduml
+@enduml
\ No newline at end of file
diff --git a/diagram/13_sequence_startup_shutdown.puml b/diagram/13_sequence_startup_shutdown.puml
index f4106e2..6448863 100644
--- a/diagram/13_sequence_startup_shutdown.puml
+++ b/diagram/13_sequence_startup_shutdown.puml
@@ -32,9 +32,10 @@ db -> db : CREATE TABLE IF NOT EXISTS\nsignal_log, signal_config
 db --> runner : DB ready
 
 loop for each CAN channel in config.can
-    runner -> reader : CANReader(bus_config, parser) [channel_i]
+    runner -> parser : DatabaseLoader.load(can_json_path) [channel_i]
+    parser --> runner : db_loader
+    runner -> reader : CANReader(bus=bus, db=db_loader, queue=rx_queue) [channel_i]
     reader -> reader : connect to CAN bus (channel_i)
-    reader -> reader : parser.load(can_json_path)\nload signal definitions from can.json
     reader --> runner : Reader #i ready
 end
 
@@ -45,7 +46,7 @@ opt simulator enabled
 end
 
 runner -> pipe : SignalPipeline(stages, store, storage)
-pipe -> pipe : add_stage(SmoothingFilter)\nadd_stage(RateLimiter)\nadd_stage(ComputedSignals)\nadd_stage(AlarmChecker)
+pipe -> pipe : add_stage(RateLimiter)\nadd_stage(ComputedSignals)\nalt alarm configs present\n  add_stage(AlarmChecker)\nend
 pipe --> runner : Pipeline ready
 
 runner -> api : create_app(config)
diff --git a/diagram/16_sequence_signal_read.puml b/diagram/16_sequence_signal_read.puml
index 82750b7..1105f68 100644
--- a/diagram/16_sequence_signal_read.puml
+++ b/diagram/16_sequence_signal_read.puml
@@ -1,5 +1,6 @@
 @startuml
 title Sequence — Signal Read Flow
+
 actor Vehicle
 participant "CAN Bus" as CANBus
 participant "CANReader" as CANReader
@@ -11,20 +12,31 @@ participant "WebSocket Manager" as WS
 participant Frontend
 
 == Frame travels from vehicle to frontend ==
-Vehicle -> CANBus: send CAN frame
-CANBus -> CANReader: deliver frame
-note right: python-can bus.recv() in executor
-CANReader -> CANReader: decode frame (DatabaseLoader)
-CANReader -> Queue: put(DecodedFrame)
-note right: CANReader writes;
-Pipeline reads batch
-Queue -> Pipeline: get batch (up to batch_drain_size)
-Pipeline -> Store: update(signal_name, value)
+Vehicle -> CANBus : send CAN frame
+CANBus -> CANReader : deliver frame
+
+note right
+  python-can bus.recv() in executor
+end note
+
+CANReader -> CANReader : decode frame (DatabaseLoader)
+CANReader -> Queue : put(DecodedFrame)
+
+note right
+  CANReader writes;
+  Pipeline reads batch
+end note
+
+Queue -> Pipeline : get batch (up to batch_drain_size)
+Pipeline -> Store : update(signal_name, value)
+
 activate Store
-Store -> WS: notify subscribers (signal update)
+Store -> WS : notify subscribers (signal update)
 deactivate Store
-Pipeline -> Repo: buffer and batch-insert(signal records)
-Repo -> Repo: persist to DB (async)
-WS -> Frontend: broadcast {type: "signal", signal, value, timestamp}
+
+Pipeline -> Repo : buffer and batch-insert(signal records)
+Repo -> Repo : persist to DB (async)
+
+WS -> Frontend : broadcast {type: "signal", signal, value, timestamp}
 
 @enduml
diff --git a/diagram/17_sequence_signal_write.puml b/diagram/17_sequence_signal_write.puml
index e7ade82..2e63979 100644
--- a/diagram/17_sequence_signal_write.puml
+++ b/diagram/17_sequence_signal_write.puml
@@ -1,5 +1,6 @@
 @startuml
 title Sequence — Signal Write (Write-back) Flow
+
 actor User
 participant Frontend
 participant "FastAPI (signals)" as API
@@ -8,18 +9,25 @@ participant "CAN Bus" as CANBus
 participant ECU
 
 == User requests a signal write in UI ==
-User -> Frontend: enter value & click Send
-Frontend -> API: PUT /signals/{signal_name} (value)
-API -> API: validate request (auth + writable + range)
+User -> Frontend : enter value & click Send
+Frontend -> API : PUT /signals/{signal_name} (value)
+
+API -> API : validate request (auth + writable + range)
+
 alt valid
-  API -> Writer: send_signal(signal_name, value)
-  note right of Writer : CANWriterRouter routes to the
-  correct CANWriter by signal name
-  Writer -> CANBus: transmit CAN frame
-  CANBus -> ECU: receive frame
-  API -> Frontend: 200 OK
+  API -> Writer : send_signal(signal_name, value)
+
+  note right of Writer
+    CANWriterRouter routes to the
+    correct CANWriter by signal name
+  end note
+
+  Writer -> CANBus : transmit CAN frame
+  CANBus -> ECU : receive frame
+  API -> Frontend : 200 OK
+
 else invalid
-  API -> Frontend: 4xx error
+  API -> Frontend : 4xx error
 end
 
 @enduml
diff --git a/diagram/20_sequence_restraints.puml b/diagram/20_sequence_restraints.puml
new file mode 100644
index 0000000..8e54dca
--- /dev/null
+++ b/diagram/20_sequence_restraints.puml
@@ -0,0 +1,66 @@
+
+@startuml
+!theme plain
+title Restraints: Match Video + Serve File
+
+actor "Frontend / Client" as client
+participant "FastAPI\n(routes/restraints.py)" as api
+participant "Filesystem\n(MEDIA_DIR)" as fs
+participant "Helper fns\n(_parse_video_filename, _score_match)" as helper
+
+== 1. Find best matching video ==
+client -> api : GET /api/restraints/match?seat=driver&seat_belt=SLL&crash_pulse=OLC18
+activate api
+
+api -> fs : if not MEDIA_DIR.exists() -> 500
+alt media dir missing
+    api --> client : 500 Media directory not found
+    deactivate api
+end
+
+api -> fs : iterate MEDIA_DIR.iterdir()
+loop for each file
+    fs --> api : filename
+    api -> helper : _parse_video_filename(filename)
+    alt parse OK
+        helper --> api : metadata dict (percentile, seat_position, weight, seat_belt)
+        api -> helper : _score_match(video_meta, requested_seat_belt, preferred_position, target_percentile)
+        helper --> api : score (float)
+    else parse fail
+        helper --> api : None (skip file)
+    end
+end
+
+api -> api : select best_video by highest score
+alt no videos or best_score <= 0
+    api --> client : {matched: false, video: null, score: 0}
+else
+    api --> client : {matched: true, video:{filename, percentile, seat_position, weight, seat_belt, url:'/api/restraints/video/<filename>'}, score}
+end
+deactivate api
+
+== 2. Client requests video file ==
+client -> api : GET /api/restraints/video/{filename}
+activate api
+
+api -> api : validate filename (no '..' or path sep)
+alt invalid filename
+    api --> client : 400 Invalid filename
+    deactivate api
+end
+
+api -> fs : file_path = MEDIA_DIR / filename
+alt file not found
+    api --> client : 404 Video not found
+else file exists
+    api -> api : resolve media_type by suffix
+    api --> client : FileResponse(path=file_path, media_type)
+end
+deactivate api
+
+note over api, helper
+  Key behavior: filename parsing based solely on filename pattern; scoring deterministic; no DB used.
+  Security: path-traversal prevented by simple validation (reject '..','/','\\').
+end note
+
+@enduml
